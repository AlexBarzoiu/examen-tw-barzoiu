import React from "react";

const Header = ({toggleMode, userEmail}) => {
    return(
        <div className="header">
            <h1>Favourite List Website</h1>
        </div>
    )
}

export default Header