import React from "react";

const SearchBar = ({toggleMode, userEmail}) => {
    return(
        <div className="search-bar">
            <h1>Search</h1>
        </div>
    )
}

export default SearchBar